export const Header = ({title}) => {
    return (
        <>
            <div className="container mb-4">
                <div className="text-center">
                    <h1>{title}</h1>
                </div>
            </div>
        </>
    )
}